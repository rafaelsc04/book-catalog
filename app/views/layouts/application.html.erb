<!DOCTYPE html>
<html>
  <head>
    <title>Isbn</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= yield %>
    <% unless flash.empty? %>
      <div data-controller="toasts" class="toast align-items-center position-absolute bottom-0 end-0 mx-4 mb-4" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
          <div class="toast-body">
            <% if flash[:notice] %>
              ⭐ <%= flash[:notice] %>
            <% elsif flash[:alert] %>
              ⚠️  <%= flash[:alert] %>
            <% end %>
          </div>
          <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
      </div>
    <% end %>
  </body>
</html>
